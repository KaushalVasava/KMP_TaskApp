CREATE TABLE Task (
    id INTEGER NOT NULL PRIMARY KEY,
    title TEXT NOT NULL
);

getTasks:
SELECT id,title FROM Task;

insertTask:
    INSERT OR REPLACE INTO Task(id, title)
    VALUES(?, ?);

deleteTask:
DELETE FROM Task
WHERE id = ?;